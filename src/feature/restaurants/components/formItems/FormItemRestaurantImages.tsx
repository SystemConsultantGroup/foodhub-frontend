import styled from "@emotion/styled";

const FormItemRestaurantImages = () => {
  return <EmotionWrapper>FormItemRestaurantImages</EmotionWrapper>;
};

export default FormItemRestaurantImages;

const EmotionWrapper = styled.div``;
