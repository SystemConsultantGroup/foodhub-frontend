import styled from "@emotion/styled";

interface Props {
  isEditMode?: boolean;
}

const RestaurantForm = ({ isEditMode = false }: Props) => {
  return <EmotionWrapper>RestaurantForm</EmotionWrapper>;
};

export default RestaurantForm;

const EmotionWrapper = styled.div``;
