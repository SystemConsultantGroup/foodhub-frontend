import styled from "@emotion/styled";
import RestaurantForm from "feature/restaurants/components/RestaurantForm";

const ViewRestaurantCreate = () => {
  return (
    <EmotionWrapper>
      <RestaurantForm />
    </EmotionWrapper>
  );
};

export default ViewRestaurantCreate;

const EmotionWrapper = styled.div``;
