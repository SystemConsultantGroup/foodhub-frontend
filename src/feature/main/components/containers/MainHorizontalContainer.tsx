import styled from "@emotion/styled";

interface Props {}

const MainHorizontalContainer = ({}: Props) => {
  return <EmotionWrapper>MainHorizontalContainer</EmotionWrapper>;
};

export default MainHorizontalContainer;

const EmotionWrapper = styled.div``;
