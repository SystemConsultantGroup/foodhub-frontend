import styled from "@emotion/styled";

interface Props {
  isEditMode?: boolean;
}

const OrganizationForm = ({ isEditMode = false }: Props) => {
  return <EmotionWrapper>OrganizationForm</EmotionWrapper>;
};

export default OrganizationForm;

const EmotionWrapper = styled.div``;
